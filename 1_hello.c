#include <stdio.h>

int main()
{
    int a, b, sum;
    printf("\n Enter the values for a: ");
    scanf("%d", &a);
    printf("\n Enter the values for b: ");
    scanf("%d", &b);
    sum = a + b;
    printf("\nThe sum is %d\n", sum);
}
